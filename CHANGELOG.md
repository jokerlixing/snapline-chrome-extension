# 更新日志

本文件记录每个已发布版本的变更。版本号写在 `extension/manifest.json`、`package.json` 和工作台侧栏里，`tests/release-contract.test.js` 会断言这几处一致。

每个版本都有同名的 Git 标签（例如 `v1.2.2`）。插件安装包发布在 [GitHub Releases](https://github.com/jokerlixing/snapline-chrome-extension/releases/latest)，网页版部署在 [GitHub Pages](https://jokerlixing.github.io/snapline-chrome-extension/)。

## 1.2.2 — 2026-09-18

### 修复

- **界面抖动**：通知（导出完成提示、网页版说明条、警告框）是普通文档流元素，出现时会把页面加高。当页面高度刚好卡在根滚动条临界点时，加高会让滚动条突然出现，视口宽度少 15 px，整列内容和预览图跟着重排——表现为"弹通知页面就抖"。现在根元素和预览区都预留滚动条槽位（`scrollbar-gutter: stable`），实测通知出现前后内容区宽度与预览图宽度变化量均为 0。此现象只在临界点触发，因此是"偶尔"出现。
- 预览图宽度原先硬编码减去 48 px（假定内边距 24 px），而窄屏断点下内边距是 16 px，导致预览图比可用宽度窄 16 px。

### 变更

- **只部署一个网站**：明确只有 `dist/site` 是网站，`dist/snapline` 与 ZIP 只作为下载包分发。仓库仅保留 `pages.yml` 一个部署工作流，只运行 `npm run build:web`，只上传 `dist/site`。
- **版本号统一**：工作台侧栏此前仍显示 `v1.2.0`、`package-lock.json` 停在 `1.2.0`，现已全部同步到 1.2.2。

### 新增

- `tests/release-contract.test.js`：断言版本号五处一致、仓库只有一个部署入口、`dist/site` 不含插件专属文件。
- 更新日志（本文件）。

### 测试

33 项单元测试、123 项浏览器检查全部通过。布局修复在有头 Chrome 下跨滚动条临界点取 8 个视口高度复测；新增断言已做负向验证（注入第二个部署工作流后测试立即失败）。

## 1.2.1 — 2026-09-18

### 修复

- **长网页生成失败**：界面直接显示 `{"code":-32000,"message":"Unable to capture screenshot"}`。原因是浏览器把一张截图渲染进单个合成表面，任一边超过 GPU 纹理上限就拒绝该请求；实测阈值为 **16,384 设备像素**，长宽两个方向都算，因此长网页和 2–3 倍清晰度都会触发。原实现只按输出文件上限（单边 32,760 px）校验，护栏形同虚设。
- 现在的处理方式：单块可容纳时仍是一次请求；超出上限时按设备像素切块、逐块截取后用 `OffscreenCanvas` 拼接（按文档坐标裁剪，不需要滚动页面）；任何一次请求被拒绝就把表面预算减半重试，最低 1,024 设备像素。整页截图与正文主区域截图共用这条路径。
- 错误提示不再泄露协议原文，改为可执行的中文说明（降低清晰度、减小自定义宽度或改用「当前可见区域」）。

### 测试

- 新增 `tests/tall-surface-e2e.mjs`（5 项）：30,000 px 长页、3× 清晰度、超宽横向分段、单表面回归、以及一次真实格式 `-32000` 的拒绝恢复。
- 真实 Chrome 153 复测：30,000 px @1×、20,000 px @1×、8,000 px @3×、12,000 px @2× 全部成功，逐行核对色带无错位；8,000 px @2× 仍保持单次请求。另用副本把预算声明为 16,385 逼出真实拒绝，插件减半后分 3 段恢复正确。

### 文档

- 复核并修正 README、根目录使用说明和详细指南中的不准确表述。

## 1.2.0 — 2026-09-11

### 新增

- **独立网页版**：本地 HTML、完整资源文件夹、粘贴代码直接生成预览，无需安装。支持自定义宽度、PNG/JPG/WebP/PDF 导出、来源重置与本机历史；不执行导入文档中的脚本。
- **GitHub Pages 部署**：推送 `main` 后自动检查、构建并发布 `dist/site`。
- 根目录使用说明（`使用说明.md`），分别介绍网页版使用与插件安装、更新。
- 项目以 MIT 许可证开源，两种构建都附带许可证和第三方依赖许可清单。

## 1.1.2 — 2026-09-11

### 修复

- 自定义宽度时恢复失效的截图后台。

## 1.1.1 — 2026-09-11

### 变更

- 移除导出设置中多余的默认文件名输入框，改用网页标题命名。

## 1.1.0 — 2026-09-11

### 新增

- 自定义预览控件，以及正文主滚动区域的完整导出。

## 1.0.0 — 2026-09-11

### 新增

- 首个版本：Chrome 插件版，把网页保存为 PNG、JPG、WebP 或截图型 PDF，并修正响应式预览问题。
