# 拾页使用说明：网页版与插件版

## 网页版：打开即用

公共地址：[https://jokerlixing.github.io/snapline-chrome-extension/](https://jokerlixing.github.io/snapline-chrome-extension/)

1. 打开网站，无需安装和登录。
2. 选择「本地 HTML」导入文件；有图片、CSS 等配套资源时，选择整个文件夹。也可以选择「粘贴 HTML」，直接粘贴代码。
3. 设置网页宽度、清晰度和截取范围，点击浅红色「生成预览」。初次使用可点「试试示例网页」。
4. 选择 PNG、JPG、WebP 或 PDF 后导出。PDF 可选自动分页或单张长页。
5. 「重置网页来源」同时清空当前来源和预览；导出设置及最近记录保留。

网页版适合静态 HTML，不执行导入文档中的脚本，嵌入的 iframe 等内容会略过。在线网站、需要登录的页面、依赖 JavaScript 的动态内容，请使用插件版。网页版不会上传文件，记录保存在当前浏览器中；它与插件版的历史互不共享。

「保持原始宽度」使用工作台当前窗口宽度；「当前可见区域」生成导入页顶部的一屏。懒加载选项只尝试加载原生懒加载图片，不会触发脚本加载更多内容。

## 插件版：保存正在浏览的网站

下载地址：[GitHub Releases](https://github.com/jokerlixing/snapline-chrome-extension/releases/latest)

1. 下载 ZIP，完整解压，保留解压后的文件夹。
2. 在 Chrome 地址栏输入 `chrome://extensions`，开启右上角「开发者模式」。
3. 点击「加载已解压的扩展程序」，选择里面直接包含 `manifest.json` 的 `snapline` 文件夹。不要选择 ZIP 或源码根目录。
4. 在浏览器工具栏固定拾页。打开目标网页并完成登录后，点击拾页图标。
5. 选择「浏览器标签页」和目标页面，设置「完整网页」或「当前可见区域」，生成预览后导出。

插件也支持输入网页链接和导入 HTML。保存 ChatGPT 类长对话时，请选择已经登录的目标标签页，等待回答完成后生成；完整截图会尝试识别并聚焦一个正文主滚动区域。小型代码框、多个独立面板和无限滚动内容不保证完整；页面仍在变化时请先等待或手动展开。本项目已验证同类滚动结构，未将私人登录会话列为已实测网站。

更新插件时，请覆盖 Chrome **当前正在加载** 的那个文件夹，在扩展管理页刷新拾页，再刷新工作台。只解压到另一个文件夹不会更新旧扩展。

## 常用设置与导出文件

- 自定义宽度支持 **200–7680 整数像素**，有 390/768/1440 px 预设和 1–3 倍清晰度。手机、平板选项只改变网页布局宽度。
- 导入文件总大小最多 **25 MB**；截图单边最多 **32,760 px**、总像素最多 **6,400 万**。过大时请降低清晰度。
- PNG、WebP 可保留原有透明区域；JPG、PDF 使用白色底。WebP 超长时等比例缩小，PDF 是不可复制文字的截图文档。
- PDF 支持 A4、Letter 自动分页或单张长页，页边距可选 0/10/20 mm。长页超限请选分页。
- 工具中没有文件名输入框，默认用网页标题命名。**插件版**会请求保存对话框，可修改名称和位置；**网页版**是否弹窗取决于浏览器下载设置，也可下载后重命名。
- 最近记录保存最近 **12 次生成的预览**，不要求已经下载。来源重置不删除历史；清空历史不删除电脑中的文件。

## 开发者在本机运行

安装 **Node.js 22 或更高版本**及 npm 后，在本文件所在目录运行：

```bash
npm ci
npm run build
npm run preview
```

浏览器访问 `http://127.0.0.1:4173` 使用本地网页版；Chrome 加载 `dist/snapline` 使用插件版。网页版部署文件在 `dist/site`。

核验功能可先运行 `npx playwright install chromium`，再运行 `npm run test:all`；升级测试需要 Git 克隆得到的完整历史。每项功能的检查结果见 [最新功能验收](docs/verification.md#2026-09-12-功能说明复核)。

更多设置和常见问题见 [详细使用指南](docs/使用指南.md)。源码及许可证见 [GitHub 开源仓库](https://github.com/jokerlixing/snapline-chrome-extension)。
