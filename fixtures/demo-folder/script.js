// This visible update verifies that a packaged local script loads correctly.
document.getElementById('demo-status').textContent = '本地脚本加载成功 · 可以开始导出';
document.documentElement.dataset.demoScript = 'loaded';
